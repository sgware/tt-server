/**
 * Story {@link World worlds} define the objects, concepts, and actions that
 * exist in a story, and story {@link WorldModel world models} define the logic
 * for when actions can happen, how actions change the current state of world,
 * when endings happen, etc.
 */
package edu.uky.cs.nil.tt.world;